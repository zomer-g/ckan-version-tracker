# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.14.0

# Auth
PyJWT[crypto]==2.10.1

# HTTP client
httpx==0.28.1

# Object storage (S3-compatible: Cloudflare R2 etc.) — used when
# STORAGE_BACKEND=r2 (app/services/storage_client.py)
boto3==1.35.99

# Scheduler
apscheduler==3.10.4

# Settings
pydantic-settings==2.7.0
pydantic[email]==2.10.4

# Diff
dictdiffer==0.9.0

# Rate limiting
slowapi==0.1.9

# Utilities
python-multipart==0.0.20

# File parsing for admin-curated odata imports (app/services/odata_import.py):
# XLSX via openpyxl, legacy XLS via xlrd, ICS/ICAL via icalendar. CSV uses the
# stdlib. Ported from the OCAL import engine.
openpyxl==3.1.5
xlrd==2.0.1
icalendar==5.0.13

# LLM (natural-language CBS search — app/api/cbs_ask.py). Provider chosen by
# whichever key is set: DeepSeek (OpenAI-compatible SDK) preferred, then Anthropic.
openai==2.26.0
anthropic==0.96.0
